@model IEnumerable<ShoppingList.Models.ListContent>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserShoppingList)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Product)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserShoppingList.ShoppingListName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Product.NameProduct)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ListContentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ListContentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ListContentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
